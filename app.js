const canvas=document.getElementById('game'),ctx=canvas.getContext('2d');
const hpEl=document.getElementById('hp'),ammoEl=document.getElementById('ammo'),scoreEl=document.getElementById('score'),timeEl=document.getElementById('time'),waveEl=document.getElementById('wave'),msg=document.getElementById('message');
let W,H,running=false,hp=100,ammo=30,maxAmmo=30,score=0,startTime,spawnTimer=0,wave=1,last=0,reloading=false,reloadTimer=0;const keys={};let player={x:0,y:0,a:0},zombies=[],turrets=[],loot=[];
function resize(){W=canvas.width=innerWidth;H=canvas.height=innerHeight}addEventListener('resize',resize);resize();
addEventListener('keydown',e=>{keys[e.key.toLowerCase()]=true;if(e.key.toLowerCase()==='r')reload();if(e.key.toLowerCase()==='t')placeTurret()});
addEventListener('keyup',e=>keys[e.key.toLowerCase()]=false);
canvas.addEventListener('mousemove',e=>{if(running&&document.pointerLockElement===canvas)player.a+=e.movementX*.003});
canvas.addEventListener('click',()=>{if(running){canvas.requestPointerLock();shoot()}});
document.getElementById('start').onclick=()=>{msg.style.display='none';reset();canvas.requestPointerLock()};
function reset(){hp=100;ammo=30;score=0;wave=1;zombies=[];turrets=[];loot=[];player={x:0,y:0,a:0};startTime=performance.now();running=true;last=performance.now();reloading=false;requestAnimationFrame(loop)}
function reload(){if(!running||reloading||ammo===maxAmmo)return;reloading=true;reloadTimer=1.2}
function placeTurret(){if(!running||turrets.length>=3)return;turrets.push({x:player.x+Math.cos(player.a)*55,y:player.y+Math.sin(player.a)*55,cool:0,life:45})}
function dropLoot(x,y){loot.push({x,y,type:Math.random()<.55?'ammo':'med',life:30})}
function shoot(){if(ammo<=0){reload();return}if(reloading)return;ammo--;for(const z of zombies){const dx=z.x-player.x,dy=z.y-player.y,d=Math.hypot(dx,dy),ang=Math.atan2(dy,dx),diff=Math.atan2(Math.sin(ang-player.a),Math.cos(ang-player.a));if(d<900&&Math.abs(diff)<.12){z.hp--;if(z.hp<=0){zombies.splice(zombies.indexOf(z),1);score+=10;if(Math.random()<.2)dropLoot(z.x,z.y)}break}}}
function spawn(){const a=Math.random()*Math.PI*2,d=600+Math.random()*900;zombies.push({x:player.x+Math.cos(a)*d,y:player.y+Math.sin(a)*d,hp:1+Math.floor(wave/4),speed:25+wave*3})}
function turretShoot(t){let best=null,bd=Infinity;for(const z of zombies){const d=Math.hypot(z.x-t.x,z.y-t.y);if(d<550&&d<bd){bd=d;best=z}}if(best){best.hp--;if(best.hp<=0){zombies.splice(zombies.indexOf(best),1);score+=15;if(Math.random()<.2)dropLoot(best.x,best.y)}return true}return false}
function collectLoot(){for(let i=loot.length-1;i>=0;i--){const l=loot[i];if(Math.hypot(l.x-player.x,l.y-player.y)<45){if(l.type==='ammo')ammo=Math.min(maxAmmo,ammo+15);else hp=Math.min(100,hp+30);score+=5;loot.splice(i,1)}}}
function loop(t){if(!running)return;const dt=Math.min((t-last)/1000,.05);last=t;const sprint=keys.shift?1.65:1,sp=150*dt*sprint;let dx=0,dy=0;if(keys.w)dy+=1;if(keys.s)dy-=1;if(keys.a)dx-=1;if(keys.d)dx+=1;const ca=Math.cos(player.a),sa=Math.sin(player.a);player.x+=(dx*ca+dy*sa)*sp;player.y+=(dy*ca-dx*sa)*sp;
if(reloading){reloadTimer-=dt;if(reloadTimer<=0){ammo=maxAmmo;reloading=false}}spawnTimer-=dt;if(spawnTimer<=0){spawn();spawnTimer=Math.max(.25,1.5-wave*.08)}wave=Math.floor((t-startTime)/30000)+1;
for(const z of zombies){const zx=player.x-z.x,zy=player.y-z.y,d=Math.hypot(zx,zy)||1;z.x+=zx/d*z.speed*dt;z.y+=zy/d*z.speed*dt;if(d<35)hp-=12*dt}
for(const tr of turrets){tr.life-=dt;tr.cool-=dt;if(tr.cool<=0){if(turretShoot(tr))tr.cool=.28;else tr.cool=.1}}turrets=turrets.filter(t=>t.life>0);for(const l of loot)l.life-=dt;loot=loot.filter(l=>l.life>0);collectLoot();
if(hp<=0){running=false;document.exitPointerLock();msg.style.display='block';msg.querySelector('h1').textContent='💀 KONIEC GRY';msg.querySelector('p').textContent=`Wynik: ${score} • Czas: ${Math.floor((t-startTime)/1000)} s`;document.getElementById('start').textContent='ZAGRAJ PONOWNIE'}draw();hpEl.textContent=Math.max(0,Math.floor(hp));ammoEl.textContent=reloading?'...':ammo;scoreEl.textContent=score;timeEl.textContent=Math.floor((t-startTime)/1000);waveEl.textContent='FALA '+wave;requestAnimationFrame(loop)}
function draw(){ctx.fillStyle='#10151b';ctx.fillRect(0,0,W,H);ctx.save();ctx.translate(W/2,H/2);ctx.fillStyle='#1b242c';for(let x=-W;x<W;x+=80)ctx.fillRect(x,-H,2,H*2);for(let y=-H;y<H;y+=80)ctx.fillRect(-W,y,W*2,2);ctx.restore();
for(const l of loot){const dx=l.x-player.x,dy=l.y-player.y,d=Math.hypot(dx,dy),rel=Math.atan2(Math.sin(Math.atan2(dy,dx)-player.a),Math.cos(Math.atan2(dy,dx)-player.a));if(Math.abs(rel)<1.15){const sx=W/2+Math.tan(rel)*400,sy=H/2,size=Math.max(10,450/d*55);ctx.fillStyle=l.type==='ammo'?'#ffd43b':'#ff5b5b';ctx.fillRect(sx-size/2,sy-size/2,size,size)}}
for(const tr of turrets){const dx=tr.x-player.x,dy=tr.y-player.y,d=Math.hypot(dx,dy),rel=Math.atan2(Math.sin(Math.atan2(dy,dx)-player.a),Math.cos(Math.atan2(dy,dx)-player.a));if(Math.abs(rel)<1.15){const sx=W/2+Math.tan(rel)*400,sy=H/2,size=Math.max(12,500/d*70);ctx.fillStyle='#4a78ff';ctx.fillRect(sx-size/2,sy-size*.5,size,size*.7);ctx.fillStyle='#aaa';ctx.fillRect(sx-size*.08,sy-size*1.2,size*.16,size*.8)}}
for(const z of zombies){const dx=z.x-player.x,dy=z.y-player.y,d=Math.hypot(dx,dy),rel=Math.atan2(Math.sin(Math.atan2(dy,dx)-player.a),Math.cos(Math.atan2(dy,dx)-player.a));if(Math.abs(rel)<1.15){const sx=W/2+Math.tan(rel)*400,sy=H/2,size=Math.max(18,600/d*120);ctx.fillStyle='#4caf50';ctx.fillRect(sx-size/2,sy-size,size,size*1.6);ctx.fillStyle='#111';ctx.fillRect(sx-size*.25,sy-size*.7,size*.12,size*.12);ctx.fillRect(sx+size*.13,sy-size*.7,size*.12,size*.12)}}ctx.fillStyle='#222';ctx.fillRect(W/2-90,H-80,180,80);ctx.fillStyle='#555';ctx.fillRect(W/2-12,H-170,24,100);ctx.fillStyle='#ddd';ctx.fillRect(W/2-3,H-170,6,80)}