const chat=document.querySelector('#chat');const form=document.querySelector('#form');const input=document.querySelector('#input');const clearBtn=document.querySelector('#clearBtn');
function addMessage(text,who='bot'){const row=document.createElement('div');row.className=`message ${who}`;const avatar=document.createElement('div');avatar.className='avatar';avatar.textContent=who==='bot'?'S':'Ty';const body=document.createElement('div');const name=document.createElement('strong');name.textContent=who==='bot'?'chatSI':'Ty';const p=document.createElement('p');p.textContent=text;body.append(name,p);row.append(avatar,body);chat.appendChild(row);chat.scrollTop=chat.scrollHeight}
function demoAnswer(text){const t=text.toLowerCase();if(t.includes('cześć')||t.includes('hej'))return 'Cześć! 👋 Jestem chatSI. Właśnie budujemy mnie od podstaw.';if(t.includes('kim jesteś'))return 'Jestem chatSI, własnym projektem AI. Moim celem jest stać się Twoim asystentem.';if(t.includes('pomoc'))return 'Na tym etapie mogę odpowiadać demonstracyjnie. Następny krok to podłączenie prawdziwego modelu AI.';return 'Odebrałem Twoją wiadomość. 🧠 Silnik prawdziwego AI jeszcze nie jest podłączony, ale interfejs już działa!';}
form.addEventListener('submit',e=>{e.preventDefault();const text=input.value.trim();if(!text)return;addMessage(text,'user');input.value='';setTimeout(()=>addMessage(demoAnswer(text),'bot'),350)});clearBtn.addEventListener('click',()=>{chat.innerHTML='';addMessage('Nowa rozmowa rozpoczęta. 🚀','bot');input.focus()});